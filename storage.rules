rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Basic security rule: only authenticated users can read and write files to storage
      allow read, write: if request.auth != null;
    }
  }
}
